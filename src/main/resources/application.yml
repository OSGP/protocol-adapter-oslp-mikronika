spring:
  application:
    name: protocol-adapter-oslp-mikronika

management:
  server:
    port: 9404
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,prometheus
