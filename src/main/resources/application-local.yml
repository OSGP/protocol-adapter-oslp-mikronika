spring:
  artemis:
    broker-url: tcp://localhost:61616
    user: artemis
    password: artemis

  dev-tools:
    restart:
      enabled: true

  flyway:
    baseline-on-migrate: true

  jpa:
    show-sql: false

server-socket:
  port: 12122
  hostName: localhost
